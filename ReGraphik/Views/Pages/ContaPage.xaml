<Page x:Class="ReGraphik.Views.Pages.ContaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Minha Conta" Background="White">

    <Page.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#4A5568"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,15,0,5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="BorderBrush" Value="#82A0C2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="BorderBrush" Value="#82A0C2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="40">
            <StackPanel MaxWidth="450" HorizontalAlignment="Left">

                <TextBlock Text="Meu Perfil" FontSize="26" FontWeight="Bold" Foreground="#1E293B"/>
                <TextBlock Text="Atualize os dados da sua conta abaixo." FontSize="14" Foreground="#64748B" Margin="0,5,0,10"/>

                <TextBlock Text="Nome Completo" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="TxtNome"/>

                <TextBlock Text="CPF" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="TxtCpf"/>

                <TextBlock Text="E-mail" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="TxtEmail"/>

                <TextBlock Text="Login" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="TxtLogin"/>

                <TextBlock Text="Nova Senha (deixe em branco para não alterar)" Style="{StaticResource LabelStyle}"/>
                <PasswordBox x:Name="TxtSenha"/>

                <Button x:Name="BtnSalvar" Content="Salvar Alterações" Height="45" Margin="0,30,0,0"
                        Background="#15529D" Foreground="White" FontSize="15" FontWeight="Bold" 
                        Cursor="Hand" BorderThickness="0"
                        Click="BtnSalvar_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>

            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>