<Window  x:Class="ReGraphik.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReGraphik.Views" 
        xmlns:vm="clr-namespace:ReGraphik.ViewModels" 
        xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Material"
        mc:Ignorable="d"
        Title="ReGraphik - Acesso" Height="680" Width="900"
        MinHeight="680" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Styles/Textos.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Styles/Inputs.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Styles/Botoes.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="#E2E8F0">
        <Border Margin="40,30,40,30" CornerRadius="16" Background="White" ClipToBounds="True">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="24" Opacity="0.15" Color="#000"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#1649a2"
                        BorderBrush="#2563eb" 
                        BorderThickness="1,1,0,1" 
                        CornerRadius="16,0,0,16">
                    <Grid Margin="36,40">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,120,100">
                            <TextBlock Text="♻️" FontSize="36" VerticalAlignment="Center" Foreground="White"/>
                            <TextBlock Text="ReGraphik" FontSize="32" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" VerticalAlignment="Center">
                            <TextBlock Text="Gestão inteligente de resíduos industriais" FontSize="18" FontWeight="Medium" Foreground="White" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,32"/>

                            <Button Content="CADASTRAR" 
                                    IsDefault="True"
                                    Command="{Binding CadastrarCommand}" 
                                    CommandParameter="{Binding ElementName=TxtSenhaLogin}" 
                                    Style="{StaticResource BtnSecondaryStyle}">           
                                <Button.ToolTip>
                                    <ToolTip Style="{StaticResource BtnTooltip}" BorderThickness="0" HasDropShadow="True">
                                        <TextBlock Text="Botão para cadastrar nova conta" FontWeight="SemiBold" FontSize="12" Margin="4,2"/>
                                    </ToolTip>
                                </Button.ToolTip>
                            </Button>
                        </StackPanel>

                        <TextBlock Grid.Row="2" Text="© 2026 ReGraphik" Foreground="#bae6fd" FontSize="11" HorizontalAlignment="Center" Margin="30"/>
                    </Grid>
                </Border>

                <Border Grid.Column="1" 
                        Background="White" 
                        BorderBrush="#CBD5E1" 
                        BorderThickness="1,1,1,1" 
                        CornerRadius="0,16,16,0" 
                        Padding="50,40">
                    <Grid>
                        <TabControl Background="Transparent" BorderThickness="0">

                            <TabItem Header="Fazer Login" Style="{StaticResource TabStyle}">
                                <StackPanel Margin="0,32,0,0">
                                    <TextBlock Text="Bem-vindo de volta!" FontSize="22" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,4"/>
                                    <TextBlock Text="Entre com suas credenciais para continuar." FontSize="13" Foreground="#64748B" Margin="0,0,0,32"/>

                                    <TextBlock Text="Login" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Margin="0,0,0,24"/>
                                    <TextBlock Style="{StaticResource LabelStyle}" Text="{Binding MensaLogin}" Margin="0,-20,0,20" Foreground="DarkRed"/>

                                    <TextBlock Text="Senha" Style="{StaticResource LabelStyle}"/>
                                    <PasswordBox x:Name="TxtSenhaLogin" Style="{StaticResource PasswordStyle}" Margin="0,0,0,36"/>
                                    <TextBlock Style="{StaticResource LabelStyle}" Text="{Binding MensaSenha}" Margin="0,-30,0,20" Foreground="DarkRed"/>

                                    <TextBlock Text="{Binding MensagemErroGeral}" 
                                               Foreground="DarkRed" 
                                               FontSize="13" 
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Center"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap"
                                               Margin="0,-16,0,16"/>

                                    <Button Content="ENTRAR" 
                                            IsDefault="True"
                                            Command="{Binding EntrarCommand}" 
                                            CommandParameter="{Binding ElementName=TxtSenhaLogin}" 
                                            Style="{StaticResource BtnPrimario}">
                                        <Button.ToolTip>
                                            <ToolTip Style="{StaticResource BtnTooltip}" BorderThickness="0" HasDropShadow="True">
                                                <TextBlock Text="Botão para logar no sistema" FontWeight="SemiBold" FontSize="12" Margin="4,2"/>
                                            </ToolTip>
                                        </Button.ToolTip>
                                    </Button>
                                </StackPanel>
                            </TabItem>

                            <TabItem Header="Criar Nova Conta" Style="{StaticResource TabStyle}">
                                <TabItem.DataContext>
                                    <vm:CadastroViewModel/>
                                </TabItem.DataContext>

                                <Grid Margin="0,24,0,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Margin="0,0,0,20">
                                        <TextBlock Text="Crie sua conta" FontSize="22" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,4"/>
                                        <TextBlock Text="Preencha os dados abaixo para se cadastrar." FontSize="13" Foreground="#64748B"/>
                                    </StackPanel>

                                    <Grid Grid.Row="1" Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Nome Completo" Style="{StaticResource LabelStyle}"/>
                                            <TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Margin="0,0,0,16"/>
                                            <TextBlock Style="{StaticResource LabelStyle}" Text="{Binding MensaNome}" Margin="0,-12,0,12" Foreground="DarkRed"/>

                                            <TextBlock Text="CPF" Style="{StaticResource LabelStyle}"/>
                                            <TextBox Text="{Binding CPF, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Margin="0,0,0,16"/>
                                            <TextBlock Style="{StaticResource LabelStyle}" Text="{Binding MensaCpf}" Margin="0,-12,0,12" Foreground="DarkRed"/>

                                            <TextBlock Text="E-mail" Style="{StaticResource LabelStyle}"/>
                                            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Margin="0,0,0,16"/>
                                            <TextBlock Style="{StaticResource LabelStyle}" Text="{Binding MensaEmail}" Margin="0,-12,0,12" Foreground="DarkRed"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Login" Style="{StaticResource LabelStyle}"/>
                                            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Margin="0,0,0,16"/>
                                            <TextBlock Style="{StaticResource LabelStyle}" Text="{Binding MensaLogin}" Margin="0,-12,0,12" Foreground="DarkRed"/>

                                            <TextBlock Text="Senha" Style="{StaticResource LabelStyle}"/>
                                            <PasswordBox x:Name="TxtSenhaCadastro" Style="{StaticResource PasswordStyle}" Margin="0,0,0,16"/>
                                            <TextBlock Style="{StaticResource LabelStyle}" Text="{Binding MensaSenha}" Margin="0,-12,0,12" Foreground="DarkRed"/>

                                            <FrameworkElement Height="50"/>
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Grid.Row="2" Margin="0,10,0,0">
                                        <TextBlock Text="{Binding MensagemErroGeral}" 
                                                   Foreground="DarkRed" 
                                                   FontSize="13" 
                                                   FontWeight="SemiBold"
                                                   HorizontalAlignment="Center"
                                                   TextAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,12"/>

                                        <Button Content="CADASTRAR" 
                                                IsDefault="True"
                                                Command="{Binding CadastrarCommand}" 
                                                CommandParameter="{Binding ElementName=TxtSenhaCadastro}" 
                                                Style="{StaticResource BtnPrimario}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource BtnTooltip}" BorderThickness="0" HasDropShadow="True">
                                                    <TextBlock Text="Botão para cadastrar uma nova conta" FontWeight="SemiBold" FontSize="12" Margin="4,2"/>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </TabItem>
                        </TabControl>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
