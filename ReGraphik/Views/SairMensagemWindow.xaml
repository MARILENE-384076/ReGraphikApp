<Window x:Class="ReGraphik.Views.SairMensagemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Width="420"
        SizeToContent="Height" 
        Background="Transparent"
        AllowsTransparency="True">

    <Grid Margin="16">
        <Border Background="White" CornerRadius="16" Padding="24">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="16" Opacity="0.12" Color="Black"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,4,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Icone -->
                    <Border Grid.Column="0" 
                            Background="#FFFBEB" 
                            BorderBrush="#FEF3C7"
                            BorderThickness="1"
                            Width="56" 
                            Height="56" 
                            CornerRadius="28" 
                            VerticalAlignment="Top" 
                            Margin="0,0,16,0">

                        <iconPacks:PackIconMaterial Kind="Logout"
                                                    Foreground="{DynamicResource LaranjaEsgColor}" 
                                                    Width="24" 
                                                    Height="24"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                    </Border>

                    <!-- Aba mensagens -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Confirmar Saída" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="{DynamicResource TextoPadraoColor}"/>

                        <TextBlock Text="Você realmente deseja encerrar sua sessão atual e voltar para a tela de login?"
                                   FontSize="14" 
                                   Foreground="{DynamicResource CinzaColor}" 
                                   TextWrapping="Wrap" 
                                   Margin="0,6,0,0" 
                                   LineHeight="20"
                                   LineStackingStrategy="BlockLineHeight"/>
                    </StackPanel>
                </Grid>

                <!-- Aba botões -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Botão cancelar -->
                    <Button Content="Cancelar" 
                            Height="38" 
                            Width="100"
                            Margin="0,0,12,0"
                            Style="{StaticResource RoundedButtonStyle}"
                            Background="White" 
                            BorderBrush="{DynamicResource CinzaClaroColor}"
                            BorderThickness="1"
                            Foreground="{DynamicResource CinzaColor}" 
                            FontWeight="SemiBold" 
                            Cursor="Hand"
                            Click="BtnCancelar_Click"/>

                    <!-- Botão sair -->
                    <Button Content="Sair" 
                            Height="38" 
                            Width="100"
                            Style="{StaticResource RoundedButtonStyle}"
                            Background="{DynamicResource AzulMedioColor}" 
                            Foreground="White" 
                            FontWeight="SemiBold" 
                            BorderThickness="0" 
                            Cursor="Hand"
                            Click="BtnSair_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
