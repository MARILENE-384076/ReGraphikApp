<Window x:Class="ReGraphik.Views.MensagemPdfWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Width="440"
        SizeToContent="Height" 
        Background="Transparent"
        AllowsTransparency="True">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Styles/Textos.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Styles/Botoes.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Border Background="White" CornerRadius="16" Padding="24">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="16" Opacity="0.12" Color="Black"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,4,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border x:Name="IconBorder" Grid.Column="0" 
                            Background="#FFFBEB" 
                            BorderBrush="#FEF3C7"
                            BorderThickness="1"
                            Width="56" 
                            Height="56" 
                            CornerRadius="28" 
                            VerticalAlignment="Top" 
                            Margin="0,0,16,0">

                        <iconPacks:PackIconMaterial x:Name="MessageIcon"
                                                    Kind="AlertCircle"
                                                    Foreground="{DynamicResource LaranjaEsgColor}" 
                                                    Width="24" 
                                                    Height="24"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtTitulo" 
                                   Text="Título da Mensagem" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="{DynamicResource TextoPadraoColor}"/>

                        <TextBlock x:Name="TxtMensagem" 
                                   Text="Descrição detalhada do aviso ou do status da operação."
                                   FontSize="14" 
                                   Foreground="{DynamicResource CinzaColor}" 
                                   TextWrapping="Wrap" 
                                   Margin="0,6,0,0" 
                                   LineHeight="20"
                                   LineStackingStrategy="BlockLineHeight"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnEsquerdo" 
                            Content="Cancelar" 
                            Height="38" 
                            Width="100"
                            Margin="0,0,12,0"
                            Style="{StaticResource RoundedButtonStyle}"
                            Background="White" 
                            BorderBrush="{DynamicResource CinzaClaroColor}"
                            BorderThickness="1"
                            Foreground="{DynamicResource CinzaColor}" 
                            FontWeight="SemiBold" 
                            Cursor="Hand"
                            Click="BtnEsquerdo_Click"/>

                    <Button x:Name="BtnDireito" 
                            Content="Ok" 
                            Height="38" 
                            Width="100"
                            Style="{StaticResource RoundedButtonStyle}"
                            Background="{DynamicResource AzulMedioColor}" 
                            Foreground="White"       
                            FontWeight="SemiBold" 
                            BorderThickness="0" 
                            Cursor="Hand"
                            Click="BtnDireito_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
